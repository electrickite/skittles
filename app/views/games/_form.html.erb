<%= simple_form_for game do |f| %>
  <%= f.input :name %>
  <%= f.input :result, collection: game.class.results.keys.map {|key| [key.humanize, key]}, include_blank: false %>
  <%= f.input :completed_at %>
  <%= f.button :submit, 'Save' %>
<% end %>
